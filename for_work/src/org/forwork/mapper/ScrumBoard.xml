<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="org.forwork.mapper.ScrumBoardMapper">
	<cache />
	
	<insert id="insertTask" parameterType="Task">
		insert into task_ex(
		TASK_CONTENT, task_type_id, task_id
		)
		values(
		#{task_content}, 1, task_ex_seq.nextval
		)
	
	</insert>
	
	<select id="listTask"  resultType="Task">
		select task_content, responsibility, writer, task_type_id, task_id from task_ex
	</select>
	
	<delete id="deleteTask" parameterType="int">
		delete from task_ex where task_id = #{task_id}	
	</delete>
	
	<update id="updateTask" parameterType="Task">
		update task_ex set task_content=#{task_content}, responsibility=#{responsibility}
		where task_id = #{task_id}
	</update> 
	
	<select id="detailTask" resultType="Task" parameterType="int">
		select task_id, task_content, responsibility from task_ex where task_id = #{task_id}
	</select>
	
	
	
</mapper>















